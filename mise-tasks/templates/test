#!/usr/bin/env bash
#MISE description="Run bats tests for scaffold, all templates, and docker contracts"
set -euo pipefail
cd "${MISE_PROJECT_ROOT:-$PWD}"
bats --jobs 4 test/scaffold.bats test/scaffold-templates.bats test/docker.bats test/template-export.bats
